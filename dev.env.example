# Copy to dev.env (git-ignored) and adjust. Loaded by the Makefile (-include dev.env).
# For local development convenience only — never commit real secrets here.

# Address the CLI talks to by default.
# VAULT_ADDR=http://127.0.0.1:8200
# KEEPHIPPO_ADDR=http://127.0.0.1:8200

# Dev-server log level: trace|debug|info|warn|error
# KEEPHIPPO_LOG_LEVEL=debug
